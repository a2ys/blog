---
import Layout from "../../layouts/Layout.astro";
import { getEntry } from "astro:content";

const { slug } = Astro.params;
if (!slug) {
  throw new Error("Slug is missing.");
}

const entry = await getEntry("blog", slug);
if (!entry) {
  throw new Error(`Post not found for slug: ${slug}`);
}

const { Content } = await entry.render();

if (!entry) {
  throw new Error(`Post not found for slug: ${slug}`);
}

function getOrdinalSuffix(day: number) {
  if (day > 3 && day < 21) return "th";
  switch (day % 10) {
    case 1:
      return "st";
    case 2:
      return "nd";
    case 3:
      return "rd";
    default:
      return "th";
  }
}

function formatDate(date: Date) {
  const d = new Date(date);
  const day = d.getDate();
  const dayWithSuffix = day + getOrdinalSuffix(day);
  return `${dayWithSuffix} ${d.toLocaleString("default", { month: "long" })}, ${d.getFullYear()}`;
}
---

<!-- <Layout>
  <div class="relative w-full flex justify-center items-center p-32">
    <div class="w-[50%] mx-auto">
      <h1 class="text-5xl font-extrabold">{entry.data.title}</h1>
      <p class="text-md font-medium mt-4">
        {entry.data.author} • {formatDate(entry.data.date)}
      </p>
      <div class="my-16">
        <Content />
      </div>
      <footer class="mt-8">
        {
          entry.data.tags?.map((tag) => (
            <span class="text-md bg-neutral-300 font-semibold px-4 py-2 rounded-full mr-2 border-b-2 border-b-neutral-950">
              # {tag}
            </span>
          ))
        }
      </footer>
    </div>
  </div>
</Layout>

<style is:global>
  h1 {
    @apply text-4xl;
  }

  h2 {
    @apply text-3xl;
  }

  h3 {
    @apply text-2xl;
  }

  h4 {
    @apply text-xl;
  }

  h5 {
    @apply text-lg;
  }

  h6 {
    @apply text-base;
  }

  p {
    @apply text-lg font-medium;
  }

  pre {
    border-radius: 0.5rem;
    padding: 1rem;
    margin: 1rem 0;
    font-family: "Roboto Mono", monospace !important;
    @apply text-lg;
  }

  code {
    overflow-x: auto;
    font-family: "Roboto Mono", monospace !important;
    font-weight: 400;
  }
</style> -->
<Layout>
  <div class="w-[40%] mx-auto py-16">
    <div class="text-center mb-8">
      <h1 class="text-4xl font-extrabold">{entry.data.title}</h1>
      <p class="text-xl">
        {entry.data.author} • {formatDate(entry.data.date)}
      </p>
    </div>
    <div class="post-body">
      <Content />
    </div>
    <footer class="mt-12 text-center">
      {
        entry.data.tags?.map((tag) => (
          <span class="bg-neutral-300 px-4 py-2 rounded-xl mr-4"># {tag}</span>
        ))
      }
    </footer>
  </div>
</Layout>

<style is:global>
  .post-body {
    margin-top: 2rem;
    line-height: 1.6;
  }

  h1 {
    font-size: 2.5rem;
    font-weight: 800;
    margin: 1rem 0;
  }

  h2 {
    font-size: 2rem;
    font-weight: 800;
    margin: 1rem 0;
  }

  h3 {
    font-size: 1.75rem;
    font-weight: 800;
    margin: 1rem 0;
  }

  h4 {
    font-size: 1.5rem;
    font-weight: 800;
    margin: 1rem 0;
  }

  h5 {
    font-size: 1.25rem;
    font-weight: 800;
    margin: 1rem 0;
  }

  h6 {
    font-size: 1rem;
    font-weight: 800;
    margin: 1rem 0;
  }

  p {
    font-weight: 500;
    margin: 1rem 0;
  }

  pre {
    border-radius: 0.5rem;
    padding: 1rem;
    margin: 1rem 0;
    font-family: "Roboto Mono", monospace !important;
  }

  code {
    overflow-x: auto;
    font-family: "Roboto Mono", monospace !important;
    font-weight: 500;
  }

  img {
    max-width: 100%;
    margin: 1rem 0;
    border-radius: 0.5rem;
  }

  a {
    color: #3182ce;
    text-decoration: none;
    font-weight: 500;
  }

  a:hover {
    text-decoration: underline;
  }

  blockquote {
    border-left: 4px solid #3182ce;
    padding-left: 1rem;
    margin: 1rem 0;
    font-weight: 500;
  }

  blockquote p {
    margin: 0;
    font-weight: 500;
  }

  ul {
    list-style-type: disc;
    padding-left: 2rem;
    font-weight: 500;
  }

  ol {
    list-style-type: decimal;
    padding-left: 2rem;
    font-weight: 500;
  }

  li {
    margin: 0.5rem 0;
    font-weight: 500;
  }

  hr {
    border: 0;
    border-top: 1px solid #e2e8f0;
    margin: 2rem 0;
  }

  table {
    width: 100%;
    border-collapse: collapse;
    margin: 2rem 0;
    border: 1.5px solid;
    font-weight: 500;
    padding: 2rem;
  }

  th {
    text-align: left;
    padding: 0.5rem 1rem;
    border-bottom: 1.5px solid;
    font-weight: 800;
  }

  td {
    padding: 0.5rem 1rem;
    border-bottom: 1px solid;
    font-weight: 500;
  }
</style>
